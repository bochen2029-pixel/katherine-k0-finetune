# K0 v2 Project Audit Log — 2026-05-10

Comprehensive rescan of K0 v2 project after tier_1000 ship. Triggered by operator
request to surface anything out of order or that could be optimized before T2500+
generation and the planned 9B + 27B fine-tune runs.

Scope: pipeline integrity, data quality, documentation drift, script lifecycle.
Backups of all touched files in `.audit_backups_2026-05-10/` at project root.

---

## Findings (full scan output)

### 🔴 BLOCKER (would degrade T5000+ training)

**1. Held-out eval set MISSING.** `dataset/v2/held_out_eval/` is empty scaffolding.
PLAN.md commits to 100 traces; without it, T2500+ training runs are blind to
overfit/underfit. **NOT FIXED in this audit pass** (requires 100-trace generation
session — operator decision). Documented in TODO.md as gate item.

**2. Vision-honesty violations in `dpo_vision_register_001.jsonl` (2 lines).**
Line 1 had `[photo: Bo at his desk...]`; line 4 had `[photo: my mom at the
kitchen table...]`. Vision tower can't see "Bo" or "my mom" from pixels — same
issue I fixed earlier in `domain_I_001.jsonl` SFT. **FIXED.**

### 🟡 DEFECT (drift / inconsistency)

**3. PLAN.md domain percentage tables contradicted the Audio Reservation
section.** Main SFT table (line ~67) showed Domain I at 8% flat, A at 18% flat,
C at 14% flat across all tiers. Audio Reservation section (line ~193) showed
inc_003+ shift: A→16%, C→12%, I→7% to free 5% for P. The two tables were
inconsistent. The .gen_schedule.py + dataset_schedule.csv already followed the
Audio Reservation version. **FIXED:** rebuilt both SFT and DPO tables in PLAN.md
with two-percentage columns (inc_001/002 % and inc_003+ %) and recomputed
cumulative cell counts to reflect actual reallocation. CSV cross-check confirms
alignment.

**4. v1 legacy scripts at root unmarked.** `dpo_k0.py`, `finetune_k0.py`,
`prep_dataset.py` at project root are v1 (use FastModel which strips vision).
Future operator could pick wrong file. **FIXED:** added DEPRECATED v1 LEGACY
header to all three with explicit forward pointer to scripts/finetune_k0_v2.py
+ note that v2 DPO trainer doesn't yet exist. Did NOT rename the files because
README.md, RUNBOOK.md, run-cloud-runpod.sh, bootstrap-runpod.sh, CLOUD.md all
reference them by name — rename would cascade.

### 🟢 MINOR / OPTIMIZATION

**5. 1 duplicate user-prompt at tier_1000.** "good week?" appears 2× across the
1003 traces. Trivial; not worth deduplicating at this volume.

**6. No DPO-presence guard in `build_cumulative.py`.** Script silently produced
empty dpo_train.jsonl if increments were missing DPO files. **FIXED:** added
80/20 SFT/DPO ratio sanity check. Warns if ratio drifts outside 15-25%, or if
DPO is empty entirely.

**7. No v2 DPO trainer.** `scripts/finetune_k0_v2.py` is SFT-only. v1 DPO
trainer at `dpo_k0.py` uses FastModel (vision-stripping). For T2500+ training
with 80/20 SFT/DPO mix, v2 needs `scripts/dpo_k0_v2.py`. **NOT FIXED in this
audit pass** (real engineering work, requires testing on a small training run).
Documented in TODO.md as gate item with full spec.

### ✅ Clean across the board (verified, no action needed)

- 0 em-dash leaks across all 1003 SFT assistant turns + DPO chosen turns
- 0 em-dashes in non-EM-DASH DPO rejected fields
- 0 vision-honesty violations remaining (after fix #2)
- 0 JSON parse failures, 0 missing schema fields, 0 bad _type values
- Soul doc numerical anchors all harmonized (3 contradictions fixed earlier)
- CLAUDE.md, PLAN.md, trace_generation_prompt.md banned-pattern lists agree
- finetune_k0_v2.py uses FastVisionModel; merge_and_gguf.py + push_to_hf.py preserve mmproj
- 27B-ready (model-agnostic, no hardcoded 9B paths)

---

## Files touched

| File | Action | Backup |
|---|---|---|
| `dataset/v2/increment_001/dpo/dpo_vision_register_001.jsonl` | 2 vision-honesty fixes (lines 1, 4) | `.audit_backups_2026-05-10/dpo_vision_register_001.jsonl.bak` |
| `dataset/v2/PLAN.md` | SFT + DPO tables reconciled with audio-reservation | `.audit_backups_2026-05-10/PLAN.md.bak` |
| `dpo_k0.py` | Deprecation header added | `.audit_backups_2026-05-10/dpo_k0.py.bak` |
| `finetune_k0.py` | Deprecation header added | `.audit_backups_2026-05-10/finetune_k0.py.bak` |
| `prep_dataset.py` | Deprecation header added | `.audit_backups_2026-05-10/prep_dataset.py.bak` |
| `scripts/build_cumulative.py` | DPO-presence guard added | `.audit_backups_2026-05-10/build_cumulative.py.bak` |
| `dataset/v2/TODO.md` | Two new gate entries (v2 DPO trainer, held-out eval) | (in-place edit, no separate backup; previous content preserved as-is below the new entries) |
| `dataset/v2/AUDIT_LOG_2026-05-10.md` | NEW (this file) | n/a |

All backups at `.audit_backups_2026-05-10/` are byte-identical pre-edit copies.
To revert any single change: `cp .audit_backups_2026-05-10/<file>.bak <original-path>`.

---

## Verification after edits

- `dpo_vision_register_001.jsonl`: re-ran vision-honesty regex — 0 violations remaining
- `PLAN.md`: cross-checked against `dataset_schedule.csv` (which is already produced by .gen_schedule.py with the audio-reservation percentages baked in). CSV inc_003 SFT shows A:16% (192), C:12% (144), I:7% (84), P:5% (60). DPO shows EM-DASH:19% (57), CALLBACK:16% (48), VOICE-REGISTER:5% (15). PLAN.md tables match.
- v1 script headers: visible at top of each file, no functional change to script behavior
- `build_cumulative.py`: DPO-presence guard does not affect existing tier_500/tier_1000 builds (those have full DPO files); will warn at inc_003+ if any DPO file is missing

---

## Items deferred (intentional, operator decision needed)

1. ~~**Held-out eval set generation**~~ — **CLOSED same session.** `dataset/v2/held_out_eval/eval_traces.jsonl` generated: 100 traces, balanced inc_001/002 distribution, validator PASS.

2. ~~**v2 DPO trainer build**~~ — **CLOSED same session.** `scripts/dpo_k0_v2.py` built. FastVisionModel-based, v2 schema-aware, defaults to v2 data paths via `--tier` shorthand. NOT YET TEST-TRAINED on hardware — verify with dry run before T2500+ production training.

3. **v1 script rename** — would be cleaner end state but cascades to README.md / RUNBOOK.md / shell scripts / inter-script imports. Deprecation headers chosen as lower-risk alternative. Operator can pursue rename later if desired.

---

## Outstanding state after audit

- inc_002 + tier_1000 still COMPLETE (audit did not affect data corpus)
- All cumulative validators still PASS
- Pipeline integrity confirmed for tier_500 and tier_1000 fine-tune runs (SFT)
- Pipeline integrity for tier_2500+ fine-tune runs is GATED on (a) v2 DPO trainer build and (b) held-out eval generation

Both gates are documented in TODO.md and surfaced for operator scheduling.

---

Audit performed 2026-05-10 by Claude (Opus 4.7). Concrete findings reproduced
via Python data audit + Explore agent structural scan. All edits backed up in
`.audit_backups_2026-05-10/`. Reversible.
